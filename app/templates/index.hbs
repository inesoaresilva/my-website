<section class="home-page">
  <div class='action-message'>
    <h1>Check my latest posts</h1>
    <img role='presentation' alt='' src='../../assets/images/down-arrow.svg' />
  </div>

  <ul class='posts-list'>
    {{#each @model as |post|}}
      <PostPreview @post={{post}} />
    {{/each}}
  </ul>
</section>
